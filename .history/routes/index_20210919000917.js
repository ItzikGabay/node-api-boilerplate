const express = require('express');
const app = express();
const router = express.Router({ mergeParams: true });

routerconst router = require('express').Router()


router.get('/' , (req , res)=>{
    // router code here
})


router.get('/another-route' , (req , res)=>{
    // router code here
})

module.exports  = router

module.exports = router;